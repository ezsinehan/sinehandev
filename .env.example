# RAG API base URL (no trailing slash)
# Local: run your API with uvicorn, then use http://localhost:8000
# Production: set this in Netlify env vars to your deployed API URL
VITE_API_URL=http://localhost:8000
